# Público e incorporado ao bundle durante o build. Nunca coloque segredos aqui.
VITE_API_BASE_URL=/api/producer
# Somente desenvolvimento: destino do proxy local Vite, sem prefixo de rota.
API_PROXY_TARGET=http://127.0.0.1:8001
